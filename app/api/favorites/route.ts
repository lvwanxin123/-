import {NextResponse} from "next/server";
import {createClient} from "@/lib/supabase/server";
async function getUser(){const supabase=createClient();const {data:{user}}=await supabase.auth.getUser();return {supabase,user}}
export async function GET(){const {supabase,user}=await getUser();if(!user)return NextResponse.json({message:"请先登录。"},{status:401});const {data,error}=await supabase.from("user_favorites").select("prompt_id").eq("user_id",user.id);if(error)return NextResponse.json({message:"读取收藏失败。"},{status:500});return NextResponse.json({promptIds:(data??[]).map((x)=>x.prompt_id)})}
export async function POST(request:Request){const {supabase,user}=await getUser();if(!user)return NextResponse.json({message:"请先登录。"},{status:401});const {promptId}=await request.json();const {error}=await supabase.from("user_favorites").insert({user_id:user.id,prompt_id:promptId});if(error&&error.code!=="23505")return NextResponse.json({message:"收藏失败。"},{status:500});return NextResponse.json({ok:true})}
export async function DELETE(request:Request){const {supabase,user}=await getUser();if(!user)return NextResponse.json({message:"请先登录。"},{status:401});const {promptId}=await request.json();const {error}=await supabase.from("user_favorites").delete().eq("user_id",user.id).eq("prompt_id",promptId);if(error)return NextResponse.json({message:"取消收藏失败。"},{status:500});return NextResponse.json({ok:true})}
